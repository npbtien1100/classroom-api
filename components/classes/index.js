const express = require("express");
const router = express.Router();
import {} from "./classController";
/* GET users listing. */
router.get("/", function (req, res, next) {
  res.send("respond with a resource");
});
router.get();
router.post();
router.put();
router.delete();

module.exports = router;
